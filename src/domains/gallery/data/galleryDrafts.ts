import type { Draft } from "../../editor/components/types";
import { allPatterns } from "../../../assets/drafts";

// Gallery patterns imported from JSON assets
export const galleryDrafts: Draft[] = allPatterns;
