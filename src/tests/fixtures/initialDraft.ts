import { plainChevronPattern } from "../../assets/drafts/plain-chevron-pattern";

// Use the plain chevron pattern as the initial test fixture
const initialDraft = plainChevronPattern;

export default initialDraft;
